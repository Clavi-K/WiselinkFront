/* ----- INITIAL STATE ----- */

const initialState = {
    events: [],
    userInfo: undefined
}

/* ---------- */

/* ----- REDUCER EXPORT ----- */

export default function reducer(state = initialState, action) {

    switch (action.type) {

        default:
            return state

    }

}

/* ---------- */
